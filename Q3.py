# calculate area of triangle.

base = float(input("Enter the base of triangle:"))
height = float(input("Enter the height of triangle:"))

area = (1/2) * base * height #we can also use 0.5
print("The area of triangle is", area)
